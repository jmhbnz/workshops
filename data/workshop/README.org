#+TITLE: Openshift security workshop
#+DATE: <2026-05-11 Mon>
#+AUTHOR: James Blair


This document captures the steps required to set up an instance of the workshop.


* Log in to cluster

Let's start by logging in as the cluster admin.

#+NAME: Login to openshift cluster
#+begin_src bash
source .env
oc login "${OCP_API_URL}" --username "${OCP_USERNAME}" --password "${OCP_PASSWORD}" --insecure-skip-tls-verify
#+end_src

#+RESULTS: Login to openshift cluster
#+begin_example
WARNING: Using insecure TLS client config. Setting this option is not supported!

Login successful.

You have access to 81 projects, the list has been suppressed. You can list all projects with 'oc projects'

Using project "default".
#+end_example


* Cluster theming

To set up for this workshop and help build the style we're going to make some visual customisations to the OpenShift cluster to make it feel more like a customer production cluster.


** Update cluster logo

#+NAME: Update cluster logo
#+begin_src bash
oc create configmap console-custom-logo --from-file=/home/${USER}/Documents/workshops/public/static/images/fsi-security/logo.png --namespace openshift-config

cat << EOF | oc apply --filename -
apiVersion: operator.openshift.io/v1
kind: Console
metadata:
  name: cluster
spec:
  customization:
    customLogoFile:
      key: logo.png
      name: console-custom-logo
    customProductName: ACME Financial Services OpenShift Console
  perspectives:
    - id: admin
      visibility:
        state: Enabled
    - id: dev
      visibility:
        state: Disabled
EOF
#+end_src

#+RESULTS: Update cluster logo
#+begin_example
configmap/console-custom-logo created
console.operator.openshift.io/cluster configured
#+end_example


** Add an interesting notification banner

#+NAME: Create console banner
#+begin_src bash
cat << EOF | oc apply --filename -
apiVersion: console.openshift.io/v1
kind: ConsoleNotification
metadata:
  name: acme-banner
spec:
  text: ACME Financial Services Production OpenShift
  location: BannerTop
  color: 'darkblue'
  backgroundColor: '#fff'
EOF
#+end_src

#+RESULTS: Create console banner
#+begin_example
consolenotification.console.openshift.io/acme-banner created
#+end_example


* Create a cluster role for viewing node logs

During the workshop participants are going to query node logs with ~oc adm~.  We need to create a cluster role that will allow this.

#+NAME: Create node log view cluster role
#+begin_src bash
cat << EOF | oc apply --filename -
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: node-log-viewer
rules:
  - apiGroups: [""]
    resources:
      - "nodes/log"
      - "nodes/proxy"
      - "nodes"
    verbs: ["get", "list"]
EOF
#+end_src

#+RESULTS: Create node log view cluster role
#+begin_example
clusterrole.rbac.authorization.k8s.io/node-log-viewer configured
#+end_example


* Deploy compromised workloads

For the purposes of exercise 2 we need an existing project and demo workload for each workshop user.

We'll create the required ~Project~, ~Pod~ and ~ServiceAccount~ by hydrating an OpenShift ~Template~, then carry out the exploit with a bash script once the pods are up.

The ~setup/app-template.yaml~ file also handles the various permissions each workshop attendee requires.

#+NAME: Deploy compromised workloads
#+begin_src bash
echo "Applying template for each attendee"
for i in $(seq 1 5); do
  oc process --filename setup/app-template.yaml --param SUFFIX=$i
done | oc apply --filename -

echo "Waiting for all payment-gateway pods to initialize..."
oc wait --for=condition=Ready pods --selector app=payment-gateway --all-namespaces --timeout=90s

echo "Simulating live 'oc exec' intrusion across environments..."
for i in $(seq 1 5); do
  namespace="payment-gateway-$i"

  # Request a short lived token for our new cicd service account in this namespace
  token=$(oc create token cicd-pipeline-runner-sa --namespace "${namespace}")

  # Executing the exploit payload dynamically using explicit parameter names
  oc exec pod/payment-gateway \
    --namespace "${namespace}" \
    --token "${token}" \
    --container httpd \
    -- sh -c '
      export C2_SERVER="198.51.100.42"
      export CODENAME="SHADOW_PIVOT"
      export ENV_MARKER="ACME_SECRET_KEY_EXFIL_TEST_9921"
      mkdir -p /tmp/.hidden_toolkit
      echo "while true; do sleep 60; done" > /tmp/.hidden_toolkit/exfil.sh
      chmod +x /tmp/.hidden_toolkit/exfil.sh
      /tmp/.hidden_toolkit/exfil.sh > /dev/null 2>&1 &
    '

  echo "[+] Namespace ${namespace} weaponized via simulated session execution."
done
#+end_src

#+RESULTS: Deploy compromised workloads
#+begin_example
Applying template for each attendee
project.project.openshift.io/payment-gateway-1 unchanged
pod/payment-gateway unchanged
serviceaccount/cicd-pipeline-runner-sa unchanged
rolebinding.rbac.authorization.k8s.io/workshop-sa-edit-binding unchanged
role.rbac.authorization.k8s.io/container-executor unchanged
rolebinding.rbac.authorization.k8s.io/workshop-sa-exec-binding unchanged
rolebinding.rbac.authorization.k8s.io/attendee-admin-binding unchanged
clusterrolebinding.rbac.authorization.k8s.io/node-logs-binding-1 unchanged
project.project.openshift.io/payment-gateway-2 unchanged
pod/payment-gateway unchanged
serviceaccount/cicd-pipeline-runner-sa unchanged
rolebinding.rbac.authorization.k8s.io/workshop-sa-edit-binding unchanged
role.rbac.authorization.k8s.io/container-executor unchanged
rolebinding.rbac.authorization.k8s.io/workshop-sa-exec-binding unchanged
rolebinding.rbac.authorization.k8s.io/attendee-admin-binding unchanged
clusterrolebinding.rbac.authorization.k8s.io/node-logs-binding-2 unchanged
project.project.openshift.io/payment-gateway-3 unchanged
pod/payment-gateway unchanged
serviceaccount/cicd-pipeline-runner-sa unchanged
rolebinding.rbac.authorization.k8s.io/workshop-sa-edit-binding unchanged
role.rbac.authorization.k8s.io/container-executor unchanged
rolebinding.rbac.authorization.k8s.io/workshop-sa-exec-binding unchanged
rolebinding.rbac.authorization.k8s.io/attendee-admin-binding unchanged
clusterrolebinding.rbac.authorization.k8s.io/node-logs-binding-3 unchanged
project.project.openshift.io/payment-gateway-4 unchanged
pod/payment-gateway unchanged
serviceaccount/cicd-pipeline-runner-sa unchanged
rolebinding.rbac.authorization.k8s.io/workshop-sa-edit-binding unchanged
role.rbac.authorization.k8s.io/container-executor unchanged
rolebinding.rbac.authorization.k8s.io/workshop-sa-exec-binding unchanged
rolebinding.rbac.authorization.k8s.io/attendee-admin-binding unchanged
clusterrolebinding.rbac.authorization.k8s.io/node-logs-binding-4 unchanged
project.project.openshift.io/payment-gateway-5 unchanged
pod/payment-gateway unchanged
serviceaccount/cicd-pipeline-runner-sa unchanged
rolebinding.rbac.authorization.k8s.io/workshop-sa-edit-binding unchanged
role.rbac.authorization.k8s.io/container-executor unchanged
rolebinding.rbac.authorization.k8s.io/workshop-sa-exec-binding unchanged
rolebinding.rbac.authorization.k8s.io/attendee-admin-binding unchanged
clusterrolebinding.rbac.authorization.k8s.io/node-logs-binding-5 unchanged
Waiting for all payment-gateway pods to initialize...
pod/payment-gateway condition met
pod/payment-gateway condition met
pod/payment-gateway condition met
pod/payment-gateway condition met
pod/payment-gateway condition met
Simulating live 'oc exec' intrusion across environments...
[+] Namespace payment-gateway-1 weaponized via simulated session execution.
[+] Namespace payment-gateway-2 weaponized via simulated session execution.
[+] Namespace payment-gateway-3 weaponized via simulated session execution.
[+] Namespace payment-gateway-4 weaponized via simulated session execution.
[+] Namespace payment-gateway-5 weaponized via simulated session execution.
#+end_example


* Install web terminal operator

Participants are going to use the OpenShift Web Terminal to run commands in order to avoid having to do anything locally on their devices.

#+NAME: Install openshift web terminal
#+begin_src bash
cat << EOF | oc apply --filename -
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: web-terminal
  namespace: openshift-operators
spec:
  channel: fast
  installPlanApproval: Automatic
  name: web-terminal
  source: redhat-operators
  sourceNamespace: openshift-marketplace
EOF
#+end_src

#+RESULTS: Install openshift web terminal
#+begin_example
subscription.operators.coreos.com/web-terminal created
#+end_example


* Enable collecting externalip in rhacs

We want to show how RHACS can make external traffic visible.

#+NAME: Enable rhacs externalip
#+begin_src bash
cat << EOF | oc apply --filename -
apiVersion: v1
kind: ConfigMap
metadata:
  name: collector-config
  namespace: stackrox
data:
  runtime_config.yaml: |
    networking:
      externalIps:
        enabled: ENABLED
EOF
#+end_src

#+RESULTS: Enable rhacs externalip
#+begin_example
configmap/collector-config created
#+end_example
